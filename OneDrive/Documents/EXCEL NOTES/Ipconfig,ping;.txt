Ipconfig,ping;
Adding of Interface in Devices;
Basic Working of HUB , Switch , and Router ;
familiar with the usage of cisco packet tracer;
arp and ip command;

command
Computer Architecture :
file management , security ;
types of operating system ;
classification of Computer according to size and Speed ;
tupples and attributes;
hub switch router commands  apologies 




MACROS: a peice of code that is replaced by value of micro
#define in c;
const, static, register,global, 
/ Online C++ compiler to run C++ program online
// #include<iostream>
// using namespace std;
// class sample{
//     int a,b;
//     public:
//     void setup(){
//         a = 10;
//         b = 40;
//     }
//     friend float mean(sample s);
// };
// float mean(sample s){
//     return float(s.a+s.b)/2;
// }
// int main() {
//   sample x;
//   x.setup();
//   cout<<"mean is: "<<mean(x);
//     return 0;
// }
#include<iostream>
using namespace std;
class A;
class B;
class C;
class A{
    int a;
    public:
    void setup(){
        a = 5;
    }
    friend int mul(A,B,C);
}i;
class B{
    int b;
    public:
    void setup(){
        b = 5;
    }
    friend int mul(A,B,C);
}j;
class C{
    int c;
    public:
    void setup(){
        c = 5;
    }
    friend int mul(A,B,C);
}k;
int mul(A i,B j,C k){
    return (i.a*j.b*k.c);
}
int main(){
    i.setup();
    j.setup();
    k.setup();
    cout<<"Mul: "<<mul(i,j,k);
}









// Online C++ compiler to run C++ program online
#include <iostream>
using namespace std;
int main() {
    int a,b,c;
    cin>>a>>b;
    c = a+b;
    int* arr1= new int[a];
    int* arr2= new int[b];
    int* arr3= new int[c];
    for(int i =0;i<a;i++){
        cin>>arr1[i];
    }
    for(int i =0;i<b;i++){
        cin>>arr2[i];
    }
    int i =0;
    // int j =0;
    int k =0;
    if(arr1[i]>arr2[i]){
        while(i<b){
            arr3[i] = arr2[i];
            i++;
        }
    }
    else{
        i =b;
        while(i<c){
            arr3[i] = arr1[i-b];
            i++;
        }
    }
    cout<<i;
    cout<<endl;
    for(int i =0;i<c;i++){
        cout<<arr3[i];
    }
    return 0;
}